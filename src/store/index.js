import { createStore } from 'vuex'; // Usa createStore en lugar de Vue.use(Vuex)

const store = createStore({
  state: {
    // Estado inicial
  },
  mutations: {
    // Mutaciones
  },
  actions: {
    // Acciones
  },
  getters: {
    // Getters
  }
});

export default store;